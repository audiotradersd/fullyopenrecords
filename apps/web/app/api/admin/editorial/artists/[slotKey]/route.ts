import { cookies } from "next/headers";
import { NextResponse } from "next/server";
import { ADMIN_SESSION_COOKIE, apiProxy } from "../../../../../../lib/server-api";
export async function PUT(request: Request, { params }: { params: Promise<{ slotKey: string }> }) { try { const { slotKey } = await params; const response = await apiProxy(`/admin/editorial/artists/${slotKey}`, { method: "PUT", body: JSON.stringify(await request.json()) }, (await cookies()).get(ADMIN_SESSION_COOKIE)?.value); const body = await response.text(); return NextResponse.json(body ? JSON.parse(body) : { error: "Artists editorial API returned no response." }, { status: response.status }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Could not save artists editorial selections." }, { status: 500 }); } }
